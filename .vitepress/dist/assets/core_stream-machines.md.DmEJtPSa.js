import{_ as t,o as s,c as r,ag as a}from"./chunks/framework.DXGyWiRo.js";const u=JSON.parse('{"title":"Stream Machine Calculus — A Minimal Model for RxJS Operators","description":"","frontmatter":{"title":"Stream Machine Calculus — A Minimal Model for RxJS Operators","category":"core","tags":["core","mental-model","operators","classification","stream-machines","calculus"],"related":["operators.md","combination-operators.md","operator-policies.md","frp-concepts.md","execution-phases.md"],"sources":1,"updated":"2026-04-08T00:00:00.000Z"},"headers":[],"relativePath":"core/stream-machines.md","filePath":"core/stream-machines.md"}'),i={name:"core/stream-machines.md"};function o(n,e,d,l,c,p){return s(),r("div",null,[...e[0]||(e[0]=[a(`<h1 id="stream-machine-calculus-—-a-minimal-model-for-rxjs-operators" tabindex="-1">Stream Machine Calculus — A Minimal Model for RxJS Operators <a class="header-anchor" href="#stream-machine-calculus-—-a-minimal-model-for-rxjs-operators" aria-label="Permalink to &quot;Stream Machine Calculus — A Minimal Model for RxJS Operators&quot;">​</a></h1><blockquote><p>All RxJS operators reduce to 6 irreducible stream machines. Two vocabulary layers: a <strong>teaching DSL</strong> for explaining behaviour, and a <strong>runtime semantics DSL</strong> for analysing execution.</p></blockquote><h2 id="the-guiding-idea" tabindex="-1">The Guiding Idea <a class="header-anchor" href="#the-guiding-idea" aria-label="Permalink to &quot;The Guiding Idea&quot;">​</a></h2><p>The domain can change. The stream machines stay the same.</p><p>Every RxJS operator — regardless of domain, use case, or naming convention — is an instance of one of six fundamental machines. Learning the machines means learning the operator space once, permanently.</p><h2 id="two-vocabulary-layers" tabindex="-1">Two Vocabulary Layers <a class="header-anchor" href="#two-vocabulary-layers" aria-label="Permalink to &quot;Two Vocabulary Layers&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Layer</th><th>Purpose</th><th>Example names</th></tr></thead><tbody><tr><td><strong>Teaching</strong></td><td>Explain intent — what the operator <em>does</em> in human terms</td><td><code>when</code>, <code>evolve</code>, <code>forEach</code></td></tr><tr><td><strong>Runtime semantics</strong></td><td>Analyse execution — trigger, memory, first-emission rule</td><td><code>sampleOn</code>, <code>reduceState</code>, <code>flattenWithPolicy</code></td></tr></tbody></table><p>Both layers name the same machines. Use the teaching layer when explaining code to others. Use the runtime layer when debugging or reasoning about edge cases.</p><hr><h2 id="the-6-irreducible-machines" tabindex="-1">The 6 Irreducible Machines <a class="header-anchor" href="#the-6-irreducible-machines" aria-label="Permalink to &quot;The 6 Irreducible Machines&quot;">​</a></h2><h3 id="_1-sampleon-—-read-current-value-when-something-happens" tabindex="-1">1. <code>sampleOn</code> — Read current value when something happens <a class="header-anchor" href="#_1-sampleon-—-read-current-value-when-something-happens" aria-label="Permalink to &quot;1. \`sampleOn\` — Read current value when something happens&quot;">​</a></h3><p><strong>Teaching name:</strong> <code>when(behavior)</code></p><p><strong>Core question:</strong> When this happens, what is the current value <em>there</em>?</p><p><strong>Causal structure:</strong></p><ul><li>One privileged <strong>trigger</strong> drives time</li><li>A second stream provides <strong>remembered context</strong></li><li>Output = trigger + sampled context</li></ul><p><strong>Runtime rules:</strong></p><ul><li>Trigger fires → read latest value from context</li><li>Context changes between triggers → silently overwritten; intermediate values lost</li><li>First emission: both trigger AND context must have emitted at least once</li></ul><p><strong>RxJS operators:</strong></p><table tabindex="0"><thead><tr><th>Operator</th><th>Role of streams</th></tr></thead><tbody><tr><td><code>withLatestFrom(other$)</code></td><td>source = trigger; other$ = context</td></tr><tr><td><code>sample(notifier$)</code></td><td>notifier = trigger; source = context (roles swapped)</td></tr><tr><td><code>pairwise()</code></td><td>source = trigger; previous source value = context</td></tr><tr><td><code>distinctUntilChanged()</code></td><td>source = trigger; last emitted = context (drop if equal)</td></tr></tbody></table><p><strong>One-line reads:</strong></p><ul><li><code>withLatestFrom</code> → source fires, read the current value over there</li><li><code>sample</code> → clock fires, read the current source value</li><li><code>pairwise</code> → new value arrives, pair it with the previous</li><li><code>distinctUntilChanged</code> → new value arrives, compare with last emitted</li></ul><p><strong>Key distinction:</strong> <code>sampleOn</code> is <strong>asymmetric</strong> — the source drives time; the other stream only provides context. This is the defining difference from <code>recomputeLatest</code>.</p><hr><h3 id="_2-recomputelatest-—-recompute-whenever-any-input-changes" tabindex="-1">2. <code>recomputeLatest</code> — Recompute whenever any input changes <a class="header-anchor" href="#_2-recomputelatest-—-recompute-whenever-any-input-changes" aria-label="Permalink to &quot;2. \`recomputeLatest\` — Recompute whenever any input changes&quot;">​</a></h3><p><strong>Teaching name:</strong> <code>wheneverAny(inputs...)</code></p><p><strong>Core question:</strong> Whenever anything changes, what is the latest derived value now?</p><p><strong>Causal structure:</strong></p><ul><li>All inputs are <strong>symmetric</strong> — any can trigger</li><li>All inputs provide <strong>remembered latest values</strong></li><li>Output = recomputed from the current latest of every input</li></ul><p><strong>Runtime rules:</strong></p><ul><li>Any input fires → update its remembered latest; recompute output from all latests</li><li>Older per-input history is silently overwritten by newer latest values</li><li>First emission: every required input must have emitted at least once</li></ul><p><strong>RxJS operators:</strong> <code>combineLatest</code>, <code>combineLatestWith</code></p><p><strong>Key distinction:</strong> <code>recomputeLatest</code> is <strong>symmetric</strong> — no single input is privileged. This is the defining difference from <code>sampleOn</code>. Use <code>recomputeLatest</code>/<code>combineLatest</code> for derived state; use <code>sampleOn</code>/<code>withLatestFrom</code> for event+context reads.</p><hr><h3 id="_3-reducestate-—-evolve-remembered-state-over-time" tabindex="-1">3. <code>reduceState</code> — Evolve remembered state over time <a class="header-anchor" href="#_3-reducestate-—-evolve-remembered-state-over-time" aria-label="Permalink to &quot;3. \`reduceState\` — Evolve remembered state over time&quot;">​</a></h3><p><strong>Teaching name:</strong> <code>evolve(reducer, seed)</code></p><p><strong>Core question:</strong> Given current state and this event, what is the next state?</p><p><strong>Causal structure:</strong></p><ul><li><strong>Events</strong> drive time (the source)</li><li><strong>Current state</strong> is remembered between events</li><li>Each event → <code>reducer(currentState, event)</code> → new state → remember → emit</li></ul><p><strong>Runtime rules:</strong></p><ul><li>Source fires → compute <code>nextState = reducer(currentState, event)</code> → store → emit</li><li>Seed provides initial state before any event arrives</li><li>No values dropped — every event contributes to state in order</li><li>First emission: requires seed or initial state source</li></ul><p><strong>RxJS operators:</strong> <code>scan</code> (followed by <code>startWith(seed)</code> + <code>shareReplay(1)</code> for shared state)</p><p><strong>Key distinction from <code>sampleOn</code>:</strong></p><ul><li><code>sampleOn</code> <strong>reads</strong> current context</li><li><code>reduceState</code> <strong>changes</strong> current state — it introduces feedback memory</li></ul><p><strong>Practical form:</strong></p><div class="language-typescript vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> state$</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> events$.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pipe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">	scan</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(reducer, seed),</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">	startWith</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(seed),</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">	shareReplay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><hr><h3 id="_4-flattenwithpolicy-—-start-inner-work-choose-overlap-rule" tabindex="-1">4. <code>flattenWithPolicy</code> — Start inner work; choose overlap rule <a class="header-anchor" href="#_4-flattenwithpolicy-—-start-inner-work-choose-overlap-rule" aria-label="Permalink to &quot;4. \`flattenWithPolicy\` — Start inner work; choose overlap rule&quot;">​</a></h3><p><strong>Teaching name:</strong> <code>forEach(project, policy)</code></p><p><strong>Core question:</strong> When new work starts before old work finishes, what is the overlap rule?</p><p><strong>Causal structure:</strong></p><ul><li><strong>Outer source</strong> fires → project event into an inner Observable</li><li>Inner Observable does the actual work (HTTP, timer, etc.)</li><li><strong>Policy</strong> decides what happens if new outer events arrive while inner is still active</li></ul><p><strong>The 4 policies:</strong></p><table tabindex="0"><thead><tr><th>Policy</th><th>Teaching name</th><th>What happens when new outer event arrives</th></tr></thead><tbody><tr><td><code>merge</code></td><td><code>forEach(project, &#39;merge&#39;)</code></td><td>Allow overlap — all active inners run concurrently</td></tr><tr><td><code>switch</code></td><td><code>forEach(project, &#39;switch&#39;)</code></td><td>Cancel previous inner — only latest matters</td></tr><tr><td><code>concat</code></td><td><code>forEach(project, &#39;concat&#39;)</code></td><td>Queue new inner — run one at a time in order</td></tr><tr><td><code>exhaust</code></td><td><code>forEach(project, &#39;exhaust&#39;)</code></td><td>Ignore while busy — drop new until current completes</td></tr></tbody></table><p><strong>Memory required:</strong></p><ul><li><code>merge</code> → set of all active inner subscriptions</li><li><code>switch</code> → current active inner only</li><li><code>concat</code> → current inner + queue of pending inners</li><li><code>exhaust</code> → busy flag + current inner</li></ul><p><strong>First emission rule:</strong> outer must emit → inner must emit → output arrives</p><p><strong>One-line causal sentences:</strong></p><ul><li><code>mergeMap</code> → each event starts work, and all work may overlap</li><li><code>switchMap</code> → each event starts work, and only the latest work matters</li><li><code>concatMap</code> → each event starts work, but work runs one at a time in order</li><li><code>exhaustMap</code> → each event tries to start work, but busy work blocks new work</li></ul><p><strong>Key distinction from <code>reduceState</code>:</strong></p><ul><li><code>reduceState</code> (<code>scan</code>) stays <strong>first-order</strong> and updates state</li><li><code>flattenWithPolicy</code> starts <strong>inner Observables</strong> — higher-order execution policy</li></ul><hr><h3 id="_5-composetopology-—-arrange-multiple-sources-structurally" tabindex="-1">5. <code>composeTopology</code> — Arrange multiple sources structurally <a class="header-anchor" href="#_5-composetopology-—-arrange-multiple-sources-structurally" aria-label="Permalink to &quot;5. \`composeTopology\` — Arrange multiple sources structurally&quot;">​</a></h3><p><strong>Teaching name:</strong> <code>composeMany(sources, topology)</code></p><p><strong>Core question:</strong> How do multiple sources coexist structurally?</p><p><strong>Topologies:</strong></p><table tabindex="0"><thead><tr><th>Topology</th><th>Teaching name</th><th>Meaning</th></tr></thead><tbody><tr><td><code>merge</code></td><td><code>either(a$, b$)</code></td><td>Forward values from any source, interleaved</td></tr><tr><td><code>concat</code></td><td><code>then(a$, b$)</code></td><td>Run b$ only after a$ completes</td></tr><tr><td><code>zip</code></td><td><code>togetherByIndex(a$, b$)</code></td><td>Align nth value from each source</td></tr><tr><td><code>race</code></td><td><code>firstToWin(a$, b$)</code></td><td>First source to emit wins; others unsubscribed</td></tr><tr><td><code>forkJoin</code></td><td><code>onceAllComplete(a$, b$)</code></td><td>Emit last value from each only when all complete</td></tr></tbody></table><p><strong>Memory required:</strong></p><ul><li><code>merge</code> — none (just forward)</li><li><code>concat</code> — source ordering / current source index</li><li><code>zip</code> — per-input queues (backpressure risk)</li><li><code>race</code> — first-emission state</li><li><code>forkJoin</code> — last value from each source</li></ul><p><strong>Key distinction from <code>flattenWithPolicy</code>:</strong></p><ul><li><code>composeTopology</code> is about <strong>structural coexistence</strong> of sources — not about starting inner work per event</li><li><code>flattenWithPolicy</code> is about <strong>per-event inner work</strong> — a new inner is spawned for each outer event</li></ul><hr><h3 id="_6-shareexecution-—-control-subscription-topology-and-replay-memory" tabindex="-1">6. <code>shareExecution</code> — Control subscription topology and replay memory <a class="header-anchor" href="#_6-shareexecution-—-control-subscription-topology-and-replay-memory" aria-label="Permalink to &quot;6. \`shareExecution\` — Control subscription topology and replay memory&quot;">​</a></h3><p><strong>Teaching name:</strong> <code>shareExecution(source, memory)</code></p><p><strong>Core question:</strong> Does each subscriber get a fresh execution, or do they share one? And what past values are remembered?</p><p><strong>Two dimensions:</strong></p><ul><li><strong>Execution sharing</strong> — unicast (fresh per subscriber) vs multicast (shared)</li><li><strong>Replay memory</strong> — how many past values does a new subscriber receive?</li></ul><table tabindex="0"><thead><tr><th>Operator</th><th>Execution</th><th>Memory</th></tr></thead><tbody><tr><td><code>share()</code></td><td>Shared (multicast)</td><td>None — late subscribers get nothing</td></tr><tr><td><code>shareReplay(1)</code></td><td>Shared (multicast)</td><td>Last 1 value — late subscribers get current</td></tr><tr><td><code>shareReplay({ refCount: false })</code></td><td>Permanent (never stops)</td><td>Last n — even after all unsubscribe</td></tr><tr><td><code>BehaviorSubject</code></td><td>Shared</td><td>Current value — always has one</td></tr></tbody></table><p><strong>Runtime rule:</strong> First subscriber starts shared execution. Subsequent subscribers join. Last subscriber unsubscribes → execution stops (if <code>refCount: true</code>).</p><p><strong>Why this is its own machine:</strong> Changing from unicast to multicast changes the <strong>entire runtime graph</strong> — from per-subscriber independent execution to a single shared execution tree. This is a topology change at the subscription level.</p><hr><h2 id="derived-machines-not-irreducible" tabindex="-1">Derived Machines (Not Irreducible) <a class="header-anchor" href="#derived-machines-not-irreducible" aria-label="Permalink to &quot;Derived Machines (Not Irreducible)&quot;">​</a></h2><p>These are important — but they are specialisations of the 6 core machines:</p><table tabindex="0"><thead><tr><th>Derived machine</th><th>Teaching name</th><th>Derived from</th></tr></thead><tbody><tr><td><code>gate(condition)</code></td><td><code>filter</code>, <code>skipUntil</code>, <code>takeUntil</code></td><td><code>sampleOn</code> + boolean pass/block rule</td></tr><tr><td><code>shapeTime(policy)</code></td><td><code>debounceTime</code>, <code>throttleTime</code>, <code>delay</code></td><td>Derived timing policies (timer sources + pending memory)</td></tr></tbody></table><p><strong>Gate</strong> — values may pass only under this condition:</p><div class="language-typescript vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// gate = sampleOn + boolean memory</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">source$.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pipe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// value predicate gate</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">source$.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pipe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">takeUntil</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(destroy$))        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// lifecycle gate</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">source$.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pipe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">skipUntil</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(started$))        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// open/close gate</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p><strong>shapeTime</strong> — keep the values, change the timing rule:</p><div class="language-typescript vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">source$.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pipe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">debounceTime</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">300</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// wait for silence before emitting latest</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">source$.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pipe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">throttleTime</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// emit first, then ignore for a period</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">source$.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pipe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">auditTime</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// emit latest at window end</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">source$.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pipe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">delay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// same values, later timestamps</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><hr><h2 id="fast-classification-test" tabindex="-1">Fast Classification Test <a class="header-anchor" href="#fast-classification-test" aria-label="Permalink to &quot;Fast Classification Test&quot;">​</a></h2><p>To place any operator, ask in this order:</p><ol><li><strong>Is it mainly about reading current remembered context when something happens?</strong> → <code>sampleOn</code></li><li><strong>Is it mainly about recomputing from the latest of multiple dependencies?</strong> → <code>recomputeLatest</code></li><li><strong>Is it mainly about updating state over time?</strong> → <code>reduceState</code></li><li><strong>Is it mainly about starting inner work and choosing an overlap policy?</strong> → <code>flattenWithPolicy</code></li><li><strong>Is it mainly about how multiple sources coexist structurally?</strong> → <code>composeTopology</code></li><li><strong>Is it mainly about allowing or blocking values?</strong> → <code>gate</code> (derived from <code>sampleOn</code>)</li><li><strong>Is it mainly about changing timing without changing core value meaning?</strong> → <code>shapeTime</code> (derived)</li><li><strong>Is it mainly about shared vs fresh execution per subscriber?</strong> → <code>shareExecution</code></li></ol><hr><h2 id="complete-operator-→-machine-map" tabindex="-1">Complete Operator → Machine Map <a class="header-anchor" href="#complete-operator-→-machine-map" aria-label="Permalink to &quot;Complete Operator → Machine Map&quot;">​</a></h2><table tabindex="0"><thead><tr><th>RxJS operator</th><th>Teaching machine</th><th>Runtime machine</th><th>Trigger</th><th>Memory</th><th>Pressure policy</th></tr></thead><tbody><tr><td><code>withLatestFrom</code></td><td><code>when(...)</code></td><td><code>sampleOn</code></td><td>source</td><td>latest other</td><td>keep latest context only</td></tr><tr><td><code>sample</code></td><td><code>at(clock, value)</code></td><td><code>sampleOn</code> (roles swapped)</td><td>notifier</td><td>latest source</td><td>intermediate source values overwritten</td></tr><tr><td><code>pairwise</code></td><td>—</td><td><code>sampleOn</code> + prev memory</td><td>source</td><td>previous value</td><td>first value held as memory</td></tr><tr><td><code>distinctUntilChanged</code></td><td>—</td><td><code>sampleOn</code> + prev memory</td><td>source</td><td>last emitted</td><td>equal values dropped</td></tr><tr><td><code>combineLatest</code></td><td><code>wheneverAny(...)</code></td><td><code>recomputeLatest</code></td><td>any input</td><td>latest of all</td><td>older per-input history overwritten</td></tr><tr><td><code>filter</code></td><td><code>gate(...)</code></td><td>derived pass/block</td><td>source</td><td>predicate</td><td>failing values dropped</td></tr><tr><td><code>skipUntil</code></td><td><code>after(start$)</code></td><td>derived lifecycle gate</td><td>source (after open)</td><td>open state</td><td>pre-open values dropped</td></tr><tr><td><code>takeUntil</code></td><td><code>until(stop$)</code></td><td>derived lifecycle gate</td><td>source (until close)</td><td>closed state</td><td>post-stop values dropped</td></tr><tr><td><code>debounceTime</code></td><td><code>afterQuiet(ms)</code></td><td>derived shapeTime</td><td>source + silence</td><td>latest pending</td><td>earlier pending replaced</td></tr><tr><td><code>throttleTime</code></td><td><code>firstThenPause(ms)</code></td><td>derived shapeTime</td><td>source + window</td><td>busy window</td><td>values during window dropped</td></tr><tr><td><code>auditTime</code></td><td><code>lastAtWindowEnd(ms)</code></td><td>derived shapeTime</td><td>source + window end</td><td>latest in window</td><td>intermediate values replaced</td></tr><tr><td><code>delay</code></td><td><code>laterBy(ms)</code></td><td>derived shapeTime</td><td>source</td><td>pending delayed values</td><td>none</td></tr><tr><td><code>scan</code></td><td><code>evolve(reducer, seed)</code></td><td><code>reduceState</code></td><td>source</td><td>current state</td><td>none</td></tr><tr><td><code>mergeMap</code></td><td><code>forEach(..., &#39;merge&#39;)</code></td><td><code>flattenWithPolicy(&#39;merge&#39;)</code></td><td>source starts inner</td><td>active inners</td><td>overlap allowed</td></tr><tr><td><code>switchMap</code></td><td><code>forEach(..., &#39;switch&#39;)</code></td><td><code>flattenWithPolicy(&#39;switch&#39;)</code></td><td>source starts inner</td><td>current inner</td><td>previous inner cancelled</td></tr><tr><td><code>concatMap</code></td><td><code>forEach(..., &#39;concat&#39;)</code></td><td><code>flattenWithPolicy(&#39;concat&#39;)</code></td><td>source starts inner</td><td>current inner + queue</td><td>later work queued</td></tr><tr><td><code>exhaustMap</code></td><td><code>forEach(..., &#39;exhaust&#39;)</code></td><td><code>flattenWithPolicy(&#39;exhaust&#39;)</code></td><td>source starts inner</td><td>busy flag</td><td>new work while busy dropped</td></tr><tr><td><code>merge</code></td><td><code>either(...)</code></td><td><code>composeTopology(&#39;merge&#39;)</code></td><td>any input</td><td>none</td><td>all forwarded</td></tr><tr><td><code>concat</code></td><td><code>then(...)</code></td><td><code>composeTopology(&#39;concat&#39;)</code></td><td>current source</td><td>ordering</td><td>later sources queued</td></tr><tr><td><code>zip</code></td><td><code>togetherByIndex(...)</code></td><td><code>composeTopology(&#39;zip&#39;)</code></td><td>joint availability</td><td>per-input queues</td><td>queued until partners arrive</td></tr><tr><td><code>race</code></td><td><code>firstToWin(...)</code></td><td><code>composeTopology(&#39;race&#39;)</code></td><td>first source</td><td>first-emission state</td><td>losers unsubscribed</td></tr><tr><td><code>forkJoin</code></td><td><code>onceAllComplete(...)</code></td><td><code>composeTopology(&#39;forkJoin&#39;)</code></td><td>all complete</td><td>last value each</td><td>intermediate values dropped</td></tr><tr><td><code>share</code></td><td><code>shareWhileObserved()</code></td><td><code>shareExecution(no memory)</code></td><td>subscriptions</td><td>subscriber count</td><td>none</td></tr><tr><td><code>shareReplay</code></td><td><code>rememberLatest(n)</code></td><td><code>shareExecution(buffer n)</code></td><td>subscriptions</td><td>replay buffer</td><td>oldest beyond buffer dropped</td></tr></tbody></table><hr><h2 id="_8-classification-axes" tabindex="-1">8 Classification Axes <a class="header-anchor" href="#_8-classification-axes" aria-label="Permalink to &quot;8 Classification Axes&quot;">​</a></h2><p>For deep analysis of any operator, classify along these axes:</p><table tabindex="0"><thead><tr><th>Axis</th><th>Question</th></tr></thead><tbody><tr><td><strong>Trigger</strong></td><td>Who drives time? One privileged source? Any input? Timer? Subscriber join?</td></tr><tr><td><strong>Memory</strong></td><td>What is retained between emissions? Latest context? Accumulated state? Active inners?</td></tr><tr><td><strong>Higher-order depth</strong></td><td>Are values plain, or do they start inner Observables?</td></tr><tr><td><strong>Topology/ordering</strong></td><td>Interleaved? Sequential? By index? Raced?</td></tr><tr><td><strong>Lossiness</strong></td><td>Can values be dropped, replaced, queued, or delayed?</td></tr><tr><td><strong>Initial emission condition</strong></td><td>What must already have happened before the first output?</td></tr><tr><td><strong>Sharing</strong></td><td>Does each subscriber get fresh execution or a shared one?</td></tr><tr><td><strong>Completion/teardown</strong></td><td>What event shuts down the machine?</td></tr></tbody></table><p><strong>Recommended classification order:</strong></p><ol><li>Trigger</li><li>Memory</li><li>Higher-order depth</li><li>Topology / ordering</li><li>Lossiness / replacement / queueing</li><li>Initial emission condition</li><li>Sharing</li><li>Completion / teardown</li></ol><hr><h2 id="strongest-distinctions" tabindex="-1">Strongest Distinctions <a class="header-anchor" href="#strongest-distinctions" aria-label="Permalink to &quot;Strongest Distinctions&quot;">​</a></h2><p><strong><code>sampleOn</code> vs <code>recomputeLatest</code>:</strong></p><ul><li><code>sampleOn</code> — asymmetric; one stream drives time, the other is passive context</li><li><code>recomputeLatest</code> — symmetric; any input can trigger recomputation</li></ul><p><strong><code>gate</code> vs <code>shapeTime</code>:</strong></p><ul><li><code>gate</code> — decides <strong>whether</strong> a value may pass (boolean)</li><li><code>shapeTime</code> — decides <strong>when</strong> a value may appear (timing policy)</li></ul><p><strong><code>sampleOn</code> vs <code>reduceState</code>:</strong></p><ul><li><code>sampleOn</code> reads current context without changing it</li><li><code>reduceState</code> changes state — introduces feedback memory</li></ul><p><strong><code>flattenWithPolicy</code> vs <code>reduceState</code>:</strong></p><ul><li><code>reduceState</code> (<code>scan</code>) stays first-order; values accumulate into state</li><li><code>flattenWithPolicy</code> starts inner Observables; requires an overlap policy</li></ul><hr><h2 id="cheat-sheet" tabindex="-1">Cheat Sheet <a class="header-anchor" href="#cheat-sheet" aria-label="Permalink to &quot;Cheat Sheet&quot;">​</a></h2><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Teaching layer          Runtime layer              Core question</span></span>
<span class="line"><span>─────────────────────────────────────────────────────────────────────</span></span>
<span class="line"><span>when(behavior)          sampleOn(trigger, memory)  When this fires, what is the current value there?</span></span>
<span class="line"><span>wheneverAny(inputs)     recomputeLatest(inputs)    Whenever any input changes, what is the latest derived value?</span></span>
<span class="line"><span>evolve(reducer, seed)   reduceState(events,r,seed) Given current state + event, what is the next state?</span></span>
<span class="line"><span>forEach(project,policy) flattenWithPolicy(p,policy) New work before old finishes — overlap rule?</span></span>
<span class="line"><span>composeMany(topology)   composeTopology(s,topology) How do multiple sources coexist structurally?</span></span>
<span class="line"><span>shareExecution(memory)  shareExecution(src,memory)  Fresh execution per subscriber, or shared?</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><hr><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="./operators.html">operators</a> — full operator reference; decision tree by use case</li><li><a href="./higher-order-operators.html">higher-order-operators</a> — deep dive on all four flattening operators: marble diagrams, memory leaks, error handling, expand</li><li><a href="./combination-operators.html">combination-operators</a> — <code>composeTopology</code> machine in depth</li><li><a href="./operator-policies.html">operator-policies</a> — Eight-Policy Framework: orthogonal formal specification</li><li><a href="./frp-concepts.html">frp-concepts</a> — FRP foundations: Behavior (<code>recomputeLatest</code> semantics) vs Event (<code>sampleOn</code> semantics)</li><li><a href="./execution-phases.html">execution-phases</a> — plan phase vs execution phase; when machines activate</li></ul>`,116)])])}const m=t(i,[["render",o]]);export{u as __pageData,m as default};
